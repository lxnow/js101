let numbers = [1, 2, 3, 4];

//Write three different ways to remove all of the elements from the following array:

// for (let counter = numbers.length; counter > 0; counter--) {
//   numbers.pop();
// }

// for (let counter = numbers.length; counter > 0; counter--) {
//   numbers.shift();
// }

// numbers.splice(0);

console.log(numbers);